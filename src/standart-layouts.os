Функция JSON() Экспорт
	
	РаскладкаСообщения = Новый JSONРаскладкаСообщения;

	Возврат РаскладкаСообщения;

КонецФункции

Функция Основная() Экспорт
	
	РаскладкаСообщения = Новый ОсновнаяРаскладкаСообщения;

	Возврат РаскладкаСообщения;

КонецФункции